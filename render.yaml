# Render Blueprint Specification for Native Python Deployment
services:
  - type: web
    name: cifar10-classification-suite
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PORT
        value: 3000
